pre-commit:
  parallel: true
  commands:
    lint:
      run: pnpm exec eslint {staged_files} --cache --fix
      stage_fixed: true
      glob: '*'

commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit $1

post-merge:
  commands:
    install:
      run: pnpm install
